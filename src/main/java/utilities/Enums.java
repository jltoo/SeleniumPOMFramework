package utilities;

public class Enums {
    public enum HomePage {
        homePageText("sample"),
        homePageText2("sample2"),
        googleSearchBtn("Google Search"),
        filipinoLinkTxt("Filipino");
        public final String label;

        HomePage(String label) {
            this.label = label;
        }
    }
    
    public enum TestPage {
    	
    	
    }
}
